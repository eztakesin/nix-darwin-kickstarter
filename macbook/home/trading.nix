# Machine-local module (launchd agents for a private project).
# The real content lives only in the local working tree and is kept out
# of git via `git update-index --skip-worktree` — this committed stub is
# what everyone else sees, and it deliberately defines nothing.
{...}: {}
